=====================
= Python 3 Required =
=====================
--------------------------------------
Before running run following commands:
--------------------------------------
    pip install regex
    chmod +x modules/*.py